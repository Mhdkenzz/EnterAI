# ZAP 10049 is informational: API JSON and 404 responses intentionally use
# no-store to prevent authenticated/user-specific data from being cached.
10049	IGNORE	Intentional non-storable API responses; Cache-Control no-store is required.
